{
  "name": "nexus",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Markdown skills + Node.js MCP stdio server + CLI (zero runtime deps)",
  "scripts": {
    "validate": "node bin/nexus validate",
    "graph": "node bin/nexus graph",
    "ci": "node bin/nexus validate"
  },
  "engines": {
    "node": ">=22"
  }
}
